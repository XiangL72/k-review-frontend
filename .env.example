# Copy this file to .env and fill in your values.
# For local dev, point at your local Spring Boot backend.
# For production deploys (Vercel), set VITE_API_URL in the platform's env var settings.

VITE_API_URL=http://localhost:8080